// PostCSS-конфиг фронтенда. Tailwind CSS v4 подключается через
// плагин @tailwindcss/postcss (отдельный пакет в v4).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
